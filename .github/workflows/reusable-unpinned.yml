name: Reusable Workflow with Unpinned Action

on:
  workflow_call:

jobs:
  reusable-job:
    runs-on: ubuntu-latest
    permissions: {}
    steps:
      - uses: actions/checkout@v6
      - run: echo "reusable workflow内のステップが実行されました"
